global:
  scrape_interval: 15s
rule_files:
  - /etc/prometheus/alerts.yml
scrape_configs:
  - job_name: edurec-recommendation-api
    metrics_path: /metrics
    static_configs:
      - targets: ["recommendation-api:8000"]
