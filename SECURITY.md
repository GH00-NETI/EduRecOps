# Security policy

Do not open a public issue for vulnerabilities or sensitive data. Use GitHub private vulnerability reporting when available. This demo repository contains no production secrets; local configuration uses `.env`, while production deployments must use a secret manager. Never place learner PII in logs, metric labels, or model artifacts.
